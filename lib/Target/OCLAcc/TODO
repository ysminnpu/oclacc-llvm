Find a solution that every HW object has a pointer to its containing Block
without having to add the block_p to each and every HW constructor.
The problem is, that I hav no shared_ptr when this is done by the block itself.
